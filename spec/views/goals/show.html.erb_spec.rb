require 'rails_helper'

RSpec.describe "goals/show" do
    # let(:user) {User.create!(name:'John',password:'123456')}
    # let(:goal) {Goal.create!(title:'MyGoal',description:'bla',user_id:user.id)}
    # before(:each) do 
    #     user.reload
    #     goal.reload
    #     assign(:goal,goal)
    #     allow(view).to receive(:current_user).and_return(user)
    #     render
    # end

    # it "renders the goal's details" do 
    #     expect(rendered).to match /MyGoal/
    #     expect(rendered).to match /bla/
    #     expect(rendered).to match /Ongoing/
    #     expect(rendered).to match /Public/
    # end

    # it "has a link back to the author's show page" do 
    #     expect(rendered).to have_link 'Back', href: user_url(user)
    # end
    # it "has a link to editing the goal's details" do 
    #     expect(rendered).to have_link 'Edit', href: edit_goal_url(goal)
    # end
end